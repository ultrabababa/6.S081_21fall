{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "lab7-dev-notes",
  "steps": [
    {
      "file": "kernel/e1000.c",
      "description": "The e1000_init() function we provide you in e1000.c configures the E1000 to read packets to be transmitted from RAM, and to write received packets to RAM. This technique is called DMA, for direct memory access, referring to the fact that the E1000 hardware directly writes and reads packets to/from RAM.",
      "line": 28
    },
    {
      "file": "kernel/e1000.c",
      "description": "接收环（Receive Ring/Queue）:\n\n作用： 这是一个专门用于接收数据包的内存区域。\n\n结构： 它不是直接存储数据包本身，而是存储一个“描述符”数组。每个描述符（由 struct rx_desc 定义格式）都包含一个指向实际数据包缓冲区（即 E1000 写入接收到数据包的 RAM 地址）的指针。\n\n工作原理： 当 E1000 接收到一个数据包时，它会将数据包直接通过 DMA（直接内存访问）写入到接收环中某个描述符所指向的缓冲区。\n\n环形特性： 这是一个“循环”的数据结构。当 E1000 或驱动程序处理到数组的末尾时，它会“回绕”到数组的开头，继续使用前面的空间。这是一种高效利用内存的方式，避免了频繁的内存分配和释放。\n\nmbuf： e1000_init() 函数通过 mbufalloc() 分配 mbuf 类型的缓冲区。mbuf 是一种在网络协议栈中常用的数据结构，用于存储和管理网络数据包。",
      "line": 16
    },
    {
      "file": "kernel/e1000.c",
      "description": "发送环（Transmit Ring）:\n\n作用： 这是一个专门用于发送数据包的内存区域。\n\n工作原理： 驱动程序将它想要 E1000 发送的数据包（或其描述符）放入这个发送环中。然后 E1000 从这个环中取出数据包并进行发送。\n\n环形特性： 与接收环类似，它也是一个环形结构。",
      "line": 12
    },
    {
      "file": "kernel/e1000.c",
      "description": "“当 net.c 中的网络协议栈需要发送数据包时，它会调用 e1000_transmit()，并传入一个包含待发送数据包的 mbuf。你的发送代码必须将数据包数据的指针放入 TX（发送）环中的一个描述符中。struct tx_desc 描述了描述符的格式。你需要确保每个 mbuf 最终都会被释放，但只有在 E1000 完成数据包发送之后（E1000 会在描述符中设置 E1000_TXD_STAT_DD 位来指示这一点）。”",
      "line": 96
    },
    {
      "file": "kernel/e1000.c",
      "description": "E1000 的数据包接收过程：\n\nDMA 到 mbuf： 当 E1000 网卡从以太网接收到一个数据包时，它不会直接将数据包存储在自己的内部内存中，而是通过 DMA 技术，将数据包直接传输到主机内存中由“下一个 RX 环描述符”所指向的 mbuf（内存缓冲区）中。这种直接内存访问方式大大提高了数据传输效率，减少了 CPU 的介入。\n\n生成中断： 数据包成功 DMA 到 mbuf 后，E1000 会向主机 CPU 生成一个中断。这个中断是通知 CPU 有新的数据包到达，需要驱动程序进行处理。\n\n驱动程序 e1000_recv() 的职责：\n\n扫描 RX 环： 当 E1000 生成中断后，操作系统会调用 E1000 驱动程序中的中断处理函数，其中可能包含 e1000_recv() 的逻辑。e1000_recv() 的第一个任务就是“扫描 RX 环”。这意味着驱动程序会遍历 RX 环中的描述符，查找 E1000 已经写入了新数据包的描述符。\n\n递交到网络协议栈 (net_rx())： 对于每个发现的新数据包，e1000_recv() 负责将包含该数据包的 mbuf 传递给上层的网络协议栈（由 net.c 代表），通过调用 net_rx() 函数来实现。net_rx() 会将数据包进一步向上递交，进行协议解析（如 IP、TCP/UDP）和应用程序处理。\n\n缓冲区循环利用与重新分配： 这是非常关键的一步。由于 RX 环是“环形”的，当 E1000 使用了一个描述符所指向的 mbuf 来存储数据包后，驱动程序不能简单地丢弃这个描述符。为了让 E1000 能够在下一次循环到该位置时继续有可用的缓冲区，驱动程序必须执行以下操作：\n\n分配新的 mbuf： 分配一个新的、空的 mbuf。\n\n放置到描述符中： 将这个新分配的 mbuf 的指针（地址）更新到刚刚被 E1000 使用过的那个描述符中。这样，当 E1000 环绕一周再次回到这个描述符时，它会发现一个“新鲜的”可用缓冲区，可以继续 DMA 新到达的数据包。",
      "line": 110
    },
    {
      "file": "kernel/e1000.c",
      "description": "内存映射控制寄存器 (Memory-Mapped Control Registers)：\n\n作用： 这些寄存器是 E1000 网卡内部的特殊存储单元，它们被映射到主机 CPU 的内存地址空间中。这意味着 CPU 可以像访问普通内存一样，通过读写这些特定的内存地址来直接控制网卡的功能和获取其状态。\n\n与描述符环的区别： 描述符环主要用于数据包数据本身的传输（DMA），而控制寄存器则用于发送命令、查询状态以及同步驱动程序和硬件之间的操作。\n\n驱动程序与控制寄存器的交互目的：\n\n检测接收数据包可用性： 驱动程序需要知道 E1000 何时完成了数据包的接收并将其放入了 RX 环。虽然之前提到 E1000 会生成中断，但驱动程序通常还会检查特定的控制寄存器（例如，可能有一个指示 RX 环中新数据包数量的寄存器或指针）来确认和管理接收队列。\n\n通知 E1000 发送数据包： 当驱动程序将数据包的描述符填充到 TX 环中后，它需要通知 E1000 网卡“有新的数据包待发送了，请开始处理它们”。这个“通知”动作就是通过写入特定的控制寄存器来完成的。\n\nregs 全局变量和寄存器访问：\n\nregs：这个全局变量是一个指针，指向 E1000 网卡控制寄存器在主机内存空间中的起始地址。\n\n数组索引访问： 由于这些控制寄存器在内存中是连续排列的，驱动程序可以像访问 C 语言数组一样，通过 regs[索引] 的形式来访问不同的控制寄存器。例如，regs + offset 或 regs[index]。\n\nE1000_RDT 和 E1000_TDT 索引：\n\n这两个是特定的宏定义或常量，代表了 E1000 内部的两个重要控制寄存器在 regs 数组中的偏移量或索引。\n\nE1000_RDT (Receive Descriptor Tail)： 这个寄存器通常由驱动程序写入，用于指示接收环的尾部（即驱动程序已经处理到哪个描述符，或者 E1000 可以把下一个接收到的数据包放到哪个描述符之后）。通过更新这个寄存器，驱动程序告诉 E1000 哪些 RX 环的描述符已经准备好可以接收新的数据包了。\n\nE1000_TDT (Transmit Descriptor Tail)： 这个寄存器也是由驱动程序写入，用于指示发送环的尾部（即驱动程序已经把多少个待发送数据包的描述符填充到了 TX 环中）。通过更新这个寄存器，驱动程序告诉 E1000 网卡，从这个“尾部”开始，有新的数据包可以被取出并发送了。",
      "line": 20
    },
    {
      "file": "Makefile",
      "description": "不然新版gcc会检测出这个warning报错",
      "line": 89
    },
    {
      "file": "kernel/e1000.c",
      "description": "mbuf不应该都是直接由对应描述符结构里的指针直接指向吗，为什么还需要定义一个mbufs数组？\n\n1. 描述符 (tx_desc/rx_desc) 的作用：\n\n面向硬件： 描述符是硬件（E1000 网卡）可以直接理解和操作的结构。\n\n包含地址： 它们内部包含一个物理内存地址（或其他硬件可直接访问的地址），指向数据包实际存储的 mbuf。网卡通过这个地址来读取（发送时）或写入（接收时）数据包。\n\n状态信息： 描述符还包含硬件写入的状态信息（例如 E1000_TXD_STAT_DD），告诉驱动程序数据包是否已经发送/接收完毕。\n\n2. mbufs 数组 (tx_mbufs/rx_mbufs) 的作用：\n\n面向软件（驱动程序）： 这个数组是专门为驱动程序设计的，用于在软件层面跟踪和管理与每个描述符关联的 mbuf 指针。\n\n原因：\n\nmbuf 对象的指针： 描述符中存储的通常是 mbuf 数据内容的物理地址，而不是 mbuf 结构体本身（软件对象）的指针。虽然它们紧密相关，但从软件管理的角度，驱动程序需要访问的是整个 mbuf 结构体，以便在数据包处理完成后，能够调用 mbuffree() 来正确释放整个 mbuf 资源。\n\n释放 mbuf 的时机：\n\n发送时： 当驱动程序将一个 mbuf 提交给网卡发送后，它不能立即释放这个 mbuf。只有当网卡真正发送完数据包（通过设置描述符中的 DD 位来指示）之后，驱动程序才能安全地释放它。tx_mbufs 数组就用来保存这些待释放的 mbuf 指针。当驱动程序扫描发送环，发现某个描述符的 DD 位被设置时，它就会从 tx_mbufs 数组中取出对应索引的 mbuf 指针，然后调用 mbuffree() 释放它。\n\n接收时： 类似地，当网卡接收数据包并 DMA 到某个 mbuf 后，驱动程序会把这个 mbuf 递交给上层协议栈。为了保证接收环的持续可用性，驱动程序需要立即分配一个新的 mbuf 并更新到相应的描述符中。rx_mbufs 数组可以用来跟踪当前哪个 mbuf 正在被哪个描述符使用，或者记录那些已经被网卡填入数据并递交到上层，但其描述符位置已被新 mbuf 替换的“旧”mbuf 的信息，尽管在接收路径中，一旦 mbuf 递交上去，其生命周期就由上层接管了。但最关键的是，rx_mbufs 数组保存了驱动程序在初始化时为每个接收描述符预先分配的 mbuf 指针，以及后续每次循环重新分配的 mbuf 指针。\n\n一个简单的例子来理解 tx_mbufs：\n\n假设 TX_RING_SIZE 是 16。\n当驱动程序想发送 mbuf_A 时：\n\n它找到 tx_ring[i] 这个描述符是空的。\n\n它把 mbuf_A 的数据地址和长度填充到 tx_ring[i] 中。\n\n同时，它也会将 mbuf_A 的指针存储到 tx_mbufs[i] 中。\n\n然后它告诉网卡可以发送了。\n\n一段时间后，网卡发送完了 mbuf_A，并在 tx_ring[i] 的描述符中设置了 DD 位。\n当驱动程序下次扫描发送环时：\n\n它发现 tx_ring[i] 的 DD 位被设置了。\n\n这时，它就知道 mbuf_A 可以被释放了。但是，它不能直接从 tx_ring[i] 中获取 mbuf_A 的完整软件指针，因为描述符里只有数据地址。\n\n它会去 tx_mbufs[i] 中取出之前存储的 mbuf_A 指针。\n\n然后调用 mbuffree(tx_mbufs[i]) 来释放 mbuf_A。\n\n最后，它会分配一个新的 mbuf_B，填充到 tx_ring[i] 的数据地址字段，并将 mbuf_B 的指针存储到 tx_mbufs[i] 中，供下次使用。",
      "line": 13
    },
    {
      "file": "kernel/e1000.c",
      "description": "获取下一个发送位置 (E1000_TDT)：\n\nE1000_TDT (Transmit Descriptor Tail - 发送描述符尾部) 寄存器是由驱动程序写入的。它的值表示 E1000 网卡当前正在等待驱动程序填充新数据包描述符的环形索引。换句话说，驱动程序应该把下一个要发送的数据包的描述符放到 E1000_TDT 指向的位置。\n\n操作： 驱动程序首先读取这个寄存器来获取当前可用的、E1000 期望填充的下一个描述符的索引。",
      "line": 106
    },
    {
      "file": "kernel/e1000.c",
      "description": "检查环是否溢出（发送环满）和完成状态：\n\n溢出条件： 如果驱动程序想要填充的这个 E1000_TDT 位置上的描述符，其 E1000_TXD_STAT_DD (Descriptor Done) 位没有被设置，这表示什么？这意味着 E1000 网卡还没有完成处理之前放在这个位置的那个数据包的传输请求。换句话说，网卡还没来得及把这个“旧”的描述符标记为“完成”并清空，驱动程序却想在这个位置放置新数据。\n\n处理： 这种情况表明发送环已满（或者说，网卡的发送速度跟不上驱动程序的填充速度），或者网卡在处理上一个数据包时遇到了问题。此时，e1000_transmit() 应该返回一个错误（例如 -1），以通知调用者（网络协议栈）此次发送失败，并且这个 mbuf 未被网卡接收，需要调用者自己处理（通常是释放掉）。",
      "line": 109
    },
    {
      "file": "kernel/net.c",
      "description": "如果此次发送失败，并且这个 mbuf 未被网卡接收，调用者自己释放掉。",
      "line": 175
    },
    {
      "file": "kernel/e1000.c",
      "description": "释放旧的 mbuf：\n\n条件： 如果上一步检查通过，即 E1000_TDT 位置上的描述符已经被网卡标记为“完成”（E1000_TXD_STAT_DD 已设置），则说明这个位置已经可以被重用了。\n\n操作： 此时，驱动程序应该释放上次从这个描述符位置发送出去的那个 mbuf。这是通过 mbuffree() 函数完成的。这一步确保了发送完成的 mbuf 内存被回收，防止内存泄漏。",
      "line": 114
    },
    {
      "file": "kernel/e1000.c",
      "description": "填充新的描述符：\n\n数据源： 要发送的数据包内容在 mbuf 中，m->head 指向数据包的起始内存地址，m->len 给出数据包的长度。\n\n设置命令标志 (cmd flags)： 描述符中不仅仅包含数据地址和长度，还需要设置一些命令标志，告诉 E1000 如何处理这个数据包（例如，是否需要添加 CRC、是否是最后一个分片等）。这些标志的具体含义需要在 E1000 手册的 3.3 节中查找。\n\n保存 mbuf 指针： 驱动程序还需要在某个地方（例如，在描述符结构体中，或者一个独立的数组中）保存一个指向当前 mbuf 的指针。这是为了在将来 E1000 完成发送，E1000_TXD_STAT_DD 位被设置后，能够找到并释放这个 mbuf。",
      "line": 117
    },
    {
      "file": "kernel/e1000.c",
      "description": "m->head 指向数据包内容： 你已经知道 m->head 指向的是 mbuf 中数据包的起始内存地址（这是一个虚拟地址）。\n\nE1000 使用 DMA： E1000 网卡通过 DMA（直接内存访问）来读取数据包。硬件设备在进行 DMA 时，需要的是物理地址，而不是操作系统内部使用的虚拟地址。\n\n因此，tx_desc 中的 addr 字段应该填入 m->head 所指向的数据包内容的物理地址。",
      "line": 118
    },
    {
      "file": "kernel/e1000.c",
      "description": "Should be written with 0b for future compatibility.",
      "line": 120
    },
    {
      "file": "kernel/e1000.c",
      "description": "EOP (End Of Packet) - Bit 0: 这个位应该设置为 1b。它表示当前这个描述符是构成数据包的最后一个描述符 。对于通过一个 mbuf 发送的完整数据包，它总是最后一个。\n\nRS (Report Status) - Bit 3: 这个位应该设置为 1b。当设置此位时，硬件在处理完描述符后，会将状态写回主机内存（TDESC.STATUS 字段中的 DD 位将被设置）。这对于驱动程序了解何时可以释放 mbuf 至关重要。\n\n要理解为什么 EOP 位对于一个完整数据包的单 mbuf 发送要设置为 1b，我们需要考虑两种情况：\n\n数据包分段传输 (Packet Segmentation/Scattering)：\n\n想象一个非常大的数据包，它可能无法放入一个单独的 mbuf 缓冲区中，或者出于性能优化（如 TCP Segmentation Offload, TSO），驱动程序会将一个逻辑上的大包分成多个小的物理内存块（mbuf）。\n\n在这种情况下，驱动程序会使用多个发送描述符来描述这个逻辑上的大包。\n\n每个描述符会指向数据包中的一个分段（segment）。\n\n只有最后一个描述符的 EOP 位才会被设置为 1b。这就像在告诉网卡：“这是这个数据包的最后一个部分，收集完这个分段后，你就可以组装并发送整个数据包了。”\n\n如果中间的描述符设置了 EOP，网卡会错误地认为它收到了一个完整的数据包，并尝试发送一个不完整的数据。\n\n单个 mbuf 发送完整数据包：\n\n在我们目前讨论的简单场景中，一个 mbuf 就包含了整个待发送的数据包（例如一个以太网帧）。\n\n驱动程序只会为这个数据包创建一个单个发送描述符，这个描述符指向该 mbuf 中的数据。\n\n在这种情况下，由于这个描述符就是整个数据包的唯一描述符，也是最后一个描述符，所以 EOP 位必须设置为 1b。",
      "line": 121
    },
    {
      "file": "kernel/e1000.c",
      "description": "如果你的驱动程序没有启用硬件校验和卸载功能（即 TDESC.CMD 中的 IC 位没有设置）， 那么 CSS 和 CSO 字段对硬件来说是无关紧要的，因为硬件不会进行校验和的计算和插入。在这种情况下，将 CSS 设置为 0b（即 0）是一个安全且常见的默认做法。\n\n如果你确实启用了硬件校验和卸载功能（即 TDESC.CMD 中的 IC 位已设置）， 那么 CSS 字段就变得有意义了。手册中明确提到：“A value of 0b corresponds to the first byte in the packet.”（值为 0b 对应数据包的第一个字节）。这意味着，如果硬件要计算校验和，那么计算将从描述符中数据段的第一个字节开始。\n\n因此，无论你是否启用硬件校验和卸载，将 CSS 字段设置为 0b（即 0） 都是合适的。如果启用卸载，0b 表示从数据包开始计算；如果未启用卸载，则此字段被忽略。",
      "line": 123
    },
    {
      "file": "kernel/e1000.c",
      "description": "如果你不使用 VLAN（即 TDESC.CMD 中的 VLE 位设置为 0）， 那么 SPECIAL 字段对你的数据包传输没有意义，应该将其设置为 0。\n\n如果你需要进行 VLAN 标记的数据包传输（即 TDESC.CMD 中的 VLE 位设置为 1）， 那么 SPECIAL 字段就需要填充 VLAN 标签信息，而不仅仅是 0。",
      "line": 124
    },
    {
      "file": "kernel/e1000.c",
      "description": "保存 mbuf 指针： 驱动程序还需要在某个地方（例如，在描述符结构体中，或者一个独立的数组中）保存一个指向当前 mbuf 的指针。这是为了在将来 E1000 完成发送，E1000_TXD_STAT_DD 位被设置后，能够找到并释放这个 mbuf。",
      "line": 126
    },
    {
      "file": "kernel/e1000.c",
      "description": "初始化所有tx描述符的状态为Descriptor Done，并且对应的mbuf数据结构的虚拟地址指针设为null",
      "line": 44
    },
    {
      "file": "kernel/e1000.c",
      "description": "接收描述符数组初始化，需要真正分配内存空间，并且在描述符结构体中指出数据缓存区的首地址",
      "line": 56
    },
    {
      "file": "kernel/e1000.c",
      "description": "获取下一个待处理的接收描述符索引：\n\nE1000_RDT (Receive Descriptor Tail - 接收描述符尾部) 寄存器是由驱动程序写入的。它告诉 E1000 网卡驱动程序已经处理并释放了 RX 环直到这个索引的所有描述符，因此网卡可以将新接收到的数据包 DMA 到从这个位置开始的后续可用描述符中。\n\n要找到网卡可能刚刚写入新数据包的描述符，驱动程序需要查看 E1000_RDT 之后的那个位置。因此，操作是读取 E1000_RDT，然后加一，并进行模 RX_RING_SIZE 运算，以在环形结构中正确前进。这个结果就是驱动程序当前应该检查的描述符索引。",
      "line": 150
    },
    {
      "file": "kernel/e1000.c",
      "description": "检查新数据包是否可用：\n\nE1000_RXD_STAT_DD (Descriptor Done) 位： 这是关键的硬件状态位。当 E1000 网卡成功地将一个接收到的数据包 DMA 到某个描述符所指向的 mbuf 中后，它会在该描述符的 status 字段中设置 E1000_RXD_STAT_DD 位。\n\n判断： 驱动程序需要检查它计算出的下一个索引位置的描述符的 status 字段。如果 E1000_RXD_STAT_DD 位没有被设置，则表示该描述符尚未被硬件填充，或者没有新的数据包到来，此时驱动程序应该停止处理并等待下一次中断。",
      "line": 152
    },
    {
      "file": "kernel/e1000.c",
      "description": "处理可用数据包：\n\n更新 m->len： 如果 E1000_RXD_STAT_DD 位被设置，说明有新数据包。描述符中会包含接收到的数据包的实际长度。驱动程序需要将这个长度值更新到对应的 mbuf 的 m->len 字段中，这样上层协议栈就能知道数据包的实际大小。\n\n递交到网络协议栈 (net_rx())： 驱动程序将包含完整数据包的 mbuf 传递给上层的网络协议栈（由 net.c 中的 net_rx() 函数代表）。此时，这个 mbuf 的所有权就从驱动程序转移到了协议栈。",
      "line": 158
    },
    {
      "file": "kernel/e1000.c",
      "description": "缓冲区循环利用与描述符重置：\n\n分配新 mbuf： 既然旧的 mbuf 已经递交给了协议栈，为了确保接收环持续有可用的缓冲区供网卡使用，驱动程序必须立即为该描述符位置分配一个全新的、空的 mbuf。这通过调用 mbufalloc() 来完成。\n\n编程数据指针： 将新分配的 mbuf 的数据部分（m->head）的物理地址编程到当前描述符的地址字段中。\n\n清零描述符状态： 将该描述符的 status 字段清零。这是非常重要的一步，因为它清除了 E1000_RXD_STAT_DD 等位，表明这个描述符现在是一个“空”的、准备好接收新数据的状态，等待硬件下次写入。",
      "line": 162
    },
    {
      "file": "kernel/e1000.c",
      "description": "更新 E1000_RDT 寄存器：\n\n这是接收环同步的关键一步。驱动程序处理完一个或多个数据包并更新了相应的描述符后，它需要将 E1000_RDT 寄存器更新为它最新处理并“归还”给网卡的那个描述符的索引。\n\n这个更新告诉 E1000 网卡：“看，我（驱动程序）已经处理并清空了直到这个 RDT 位置的所有描述符，你可以把新数据包安全地写入到从这个位置开始的描述符了。” E1000 网卡内部会维护一个自己的指针（头部指针），并根据 RDT 的值来判断哪些空间是可用的。",
      "line": 169
    }
  ],
  "ref": "net"
}